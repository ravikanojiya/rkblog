export interface Iblog {
    title: string,
    id: number,
    cid: number,
    image: Array<String>,
    description: string,
    is_featured: number,
    is_active: number,
    created_at: Date,
}
